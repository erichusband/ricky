import { ReactNode } from 'react';
import { ThemeRef, TreatModule } from 'treat';
interface TreatProviderProps {
    theme: ThemeRef;
    children: ReactNode;
}
export declare const TreatProvider: ({ theme, children }: TreatProviderProps) => JSX.Element;
export declare const useTheme: () => string;
export declare const useStyles: <UserStyles extends TreatModule>(styles: UserStyles) => UserStyles;
export declare const useClassName: (classRef: string) => string;
export {};
